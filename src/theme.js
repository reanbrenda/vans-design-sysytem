import { semanticTokens } from './tokens';

const theme = {
  colors: semanticTokens.colors,
  fonts: semanticTokens.fonts,
  fontSizes: semanticTokens.fontSizes,
  lineHeights: semanticTokens.lineHeights,
  spacing: semanticTokens.spacing,
  borderRadius: semanticTokens.borderRadius,
  sizes: semanticTokens.sizes,
  grid: semanticTokens.grid,
  flex: semanticTokens.flex,
};

export default theme;
